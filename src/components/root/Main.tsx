import { Router } from '~/components/router/Router';
import { useEffect } from 'react';

function Main() {
  return (
    <main className="h-screen">
      <Router />
    </main>
  );
}

export default Main;
